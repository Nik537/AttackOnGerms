using System;
using Android.Util;
using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Graphics;
using Microsoft.Xna.Framework.Input;
using Microsoft.Xna.Framework.Input.Touch;
using System.Collections.Generic;
using AttackOnGerms.Game1Folder.RendererFolder;
using AttackOnGerms.Game1Folder.HumanPlayerFolder;
using AttackOnGerms;
namespace AttackOnGerms.Game1Folder.PhysicsFolder
{
    public class Collisions
    {
        public Collisions()
        {
            
        }
        /*
        public static checkCollisions()
        {
            foreach (Bullet bullet in Game1.controller.bullets)
            {
                Vector2 v = bullet.position - Game1.yellowEnemy.position;
                double distance = Math.Sqrt(v.X * v.X + v.Y * v.Y);
                if (distance <= (35 + 206))
                {

                }
            }
        }
        */

    }
}
